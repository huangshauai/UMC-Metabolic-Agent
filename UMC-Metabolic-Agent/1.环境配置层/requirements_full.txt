# UMC-Metabolic-Agent 完整依赖（AGI L3全能力适配，含核心+拓展）
# 核心依赖（复用requirements_core.txt，版本完全一致）
numpy==1.26.4
pandas==2.1.4
scikit-learn==1.3.2
configparser==5.3.0
scipy==1.11.4
psutil==5.9.6
python-dotenv==1.0.0

# 拓展依赖（AGI L3全能力必备）
matplotlib==3.8.2          # 结果可视化（plot_generator.py绘图）
flask==2.3.3               # API服务（custom_app_api.py多模态对接）
pillow==10.1.0             # 图片解析（multimodal_parser.py多模态）
openai-whisper==20231106   # 语音解析（multimodal_parser.py核心）
requests==2.31.0           # HTTP请求（API交互/跨域数据获取）
reportlab==4.0.7           # 自动化报告生成（report_generator.py）
tabulate==0.9.0            # 格式化输出（命令行/报告美观性）
# 系统适配依赖（仅Windows生效，Linux/macOS自动忽略）
pywin32==306; sys_platform == "win32"